name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: yarn install
    - run: yarn global add firebase
    - run: yarn global add firebase-tools
    - run: cd functions/ && npm install
    - run: yarn run build
      env:
        CI: true
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        VUE_APP_API_KEY: AIzaSyC4BmJmUbuScvS8thUE0nFgkKYfgVbBFCI
        VUE_APP_AUTH_DOMAIN: notes-4ffab.firebaseapp.com
        VUE_APP_DATA_BASE_URL: https://notes-4ffab.firebaseio.com
        VUE_APP_PROJECT_ID: notes-4ffab
        VUE_APP_STORAGE_BUCKET: notes-4ffab.appspot.com
        VUE_APP_MESSAGING_SENDER_ID: 836432019887
        VUE_APP_ID: 1:836432019887:web:fe9d404739e254912def5a
        VUE_APP_MEASUREMENT_ID: G-P1D31QCLJQ
